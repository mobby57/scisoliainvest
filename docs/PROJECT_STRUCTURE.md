# Project Structure

This document explains the organization of the "Investir à Metz" project.

## Backend

The backend is structured as follows:

- `api/`: Main API entry points
- `database/`: Database configuration and connection
- `models/`: Data models and schemas
- `routes/`: API route definitions
- `middleware/`: Custom middleware functions
- `utils/`: Utility functions
- `tests/`: Backend test files
- `factories/`: Factory functions for generating test data

## Frontend

The frontend follows a standard structure:

- `public/`: Static assets
- `src/`: Main source code
  - `assets/`: Images, icons, and other media
  - `components/`: Reusable UI components
  - `pages/`: Page components
  - `services/`: API service functions
  - `utils/`: Utility functions
  - `contexts/`: React context providers
  - `types/`: TypeScript type definitions
- `tests/`: Frontend test files

## Documentation

The `docs/` directory contains technical documentation, including:
- Project structure
- API endpoints
- Setup instructions
- Deployment guides
